#include <stdio.h>
#include <stdlib.h>

int* prepTab(int*,int);
void sort_bubble(int*, int,int*,int*);
void sort_insert(int*, int,int*,int*);
void sort_shell(int*, int,int*,int*);
void quicksort(int*, int, int,int*,int*);
